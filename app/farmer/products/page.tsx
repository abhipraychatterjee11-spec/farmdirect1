"use client";
import { FormEvent,useEffect,useState } from "react";
type Product={id:string;name:string;category:string;description:string|null;price_inr:number;unit:string;status:string;inventory:{available_quantity:number}[]};
const blank={name:"",category:"Vegetables",description:"",price_inr:"",quantity:"",unit:"kg",status:"active"};
export default function FarmerProducts(){const[items,setItems]=useState<Product[]>([]),[form,setForm]=useState<any>(blank),[editing,setEditing]=useState<string|null>(null),[loading,setLoading]=useState(true),[message,setMessage]=useState("");const load=async()=>{setLoading(true);const r=await fetch('/api/farmer/products');const d=await r.json();if(r.ok)setItems(d);else setMessage(d.error);setLoading(false)};useEffect(()=>{load()},[]);const change=(e:any)=>setForm({...form,[e.target.name]:e.target.value});async function submit(e:FormEvent){e.preventDefault();setMessage("");const body={...form,price_inr:Number(form.price_inr),quantity:Number(form.quantity),...(editing?{id:editing}:{})};const r=await fetch('/api/farmer/products',{method:editing?'PATCH':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok){setMessage(d.error);return}setMessage(editing?'Product updated.':'Product added.');setForm(blank);setEditing(null);load()}function edit(p:Product){setEditing(p.id);setForm({name:p.name,category:p.category,description:p.description||'',price_inr:String(p.price_inr),quantity:String(p.inventory[0]?.available_quantity??0),unit:p.unit,status:p.status})}async function archive(id:string){if(!confirm('Deactivate this product?'))return;const r=await fetch('/api/farmer/products',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});setMessage(r.ok?'Product deactivated.':(await r.json()).error);load()}return <main className="min-h-screen bg-[#F8FAF6]"><div className="mx-auto max-w-6xl p-5 md:p-10"><p className="text-sm font-bold text-leaf">FARMER / FPO PORTAL</p><h1 className="mt-2 text-3xl font-extrabold">Products & inventory</h1><p className="mt-2 text-slate-600">Only you can manage these listings.</p><section className="mt-7 rounded-2xl bg-white p-6 shadow-sm"><h2 className="font-bold">{editing?'Edit product':'Add a product'}</h2><form onSubmit={submit} className="mt-4 grid gap-3 sm:grid-cols-2">{[['name','Product name'],['category','Category'],['price_inr','Price (INR)'],['quantity','Available quantity']].map(([n,l])=><label key={n} className="text-sm font-semibold">{l}<input name={n} value={form[n]} onChange={change} required type={n==='price_inr'||n==='quantity'?'number':'text'} min={n==='price_inr'?'0.01':n==='quantity'?'0':undefined} step="0.01" className="mt-1 w-full rounded-lg border p-2"/></label>)}<label className="text-sm font-semibold">Unit<select name="unit" value={form.unit} onChange={change} className="mt-1 w-full rounded-lg border p-2">{['kg','quintal','piece','dozen','crate'].map(x=><option key={x}>{x}</option>)}</select></label><label className="text-sm font-semibold">Availability<select name="status" value={form.status} onChange={change} className="mt-1 w-full rounded-lg border p-2">{['active','draft','paused'].map(x=><option key={x}>{x}</option>)}</select></label><label className="text-sm font-semibold sm:col-span-2">Description<textarea name="description" value={form.description} onChange={change} className="mt-1 w-full rounded-lg border p-2"/></label><div className="flex gap-3 sm:col-span-2"><button className="rounded-xl bg-leaf px-5 py-3 text-sm font-bold text-white">{editing?'Save changes':'Add product'}</button>{editing&&<button type="button" onClick={()=>{setEditing(null);setForm(blank)}} className="rounded-xl border px-5 py-3 text-sm font-bold">Cancel</button>}</div></form>{message&&<p className="mt-4 rounded-lg bg-green-50 p-3 text-sm text-leaf">{message}</p>}</section><section className="mt-7"><h2 className="text-xl font-bold">Your listings</h2>{loading?<p className="mt-4 text-slate-500">Loading products…</p>:items.length===0?<p className="mt-4 rounded-xl bg-white p-5 text-slate-600">No products yet. Add your first listing above.</p>:<div className="mt-4 grid gap-4 md:grid-cols-2">{items.map(p=><article key={p.id} className="rounded-2xl bg-white p-5 shadow-sm"><div className="flex justify-between gap-3"><div><h3 className="font-bold">{p.name}</h3><p className="text-sm text-slate-500">{p.category} · {p.unit}</p></div><span className="rounded-lg bg-green-50 px-2 py-1 text-sm font-bold text-leaf">₹{p.price_inr}</span></div><p className="mt-3 text-sm">Available: <b>{p.inventory[0]?.available_quantity??0} {p.unit}</b> · <span className="capitalize">{p.status}</span></p><div className="mt-4 flex gap-3"><button onClick={()=>edit(p)} className="text-sm font-bold text-leaf">Edit</button>{p.status!=='archived'&&<button onClick={()=>archive(p.id)} className="text-sm font-bold text-red-600">Deactivate</button>}</div></article>)}</div>}</section></div></main>}
